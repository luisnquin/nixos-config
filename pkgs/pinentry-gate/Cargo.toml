[package]
name = "pinentry-gate"
version = "0.1.0"
edition = "2021"
description = "a pinentry that asks on every surface at once"
license = "MIT"

[dependencies]
base64 = "0.22"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
strip = true
