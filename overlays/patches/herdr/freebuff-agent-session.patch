--- a/src/agent_resume.rs
+++ b/src/agent_resume.rs
@@ -158,6 +158,16 @@
         ("herdr:droid", "droid", AgentSessionRefKind::Id) => {
             vec!["droid".into(), "--resume".into(), session_ref.value.clone()]
         }
+        ("herdr:freebuff", "freebuff", AgentSessionRefKind::Id) => {
+            // freebuff names a chat by the ISO timestamp of its first turn, not
+            // a uuid, and `--continue` with no value silently falls back to the
+            // most recently touched chat in the project. Always pass the value.
+            vec![
+                "freebuff".into(),
+                "--continue".into(),
+                session_ref.value.clone(),
+            ]
+        }
         ("herdr:kimi", "kimi", AgentSessionRefKind::Id) => {
             vec!["kimi".into(), "--session".into(), session_ref.value.clone()]
         }
@@ -270,6 +280,7 @@
             | ("herdr:copilot", "copilot")
             | ("herdr:devin", "devin")
             | ("herdr:droid", "droid")
+            | ("herdr:freebuff", "freebuff")
             | ("herdr:kimi", "kimi")
             | ("herdr:omp", "omp")
             | ("herdr:mastracode", "mastracode")
@@ -411,6 +422,16 @@
         );
         assert_eq!(
             plan(
+                "herdr:freebuff",
+                "freebuff",
+                &AgentSessionRef::id("2026-08-27T18-50-27.123Z").unwrap()
+            )
+            .unwrap()
+            .argv,
+            vec!["freebuff", "--continue", "2026-08-27T18-50-27.123Z"]
+        );
+        assert_eq!(
+            plan(
                 "herdr:kimi",
                 "kimi",
                 &AgentSessionRef::id("kimi-session").unwrap()
--- a/src/config/sound.rs
+++ b/src/config/sound.rs
@@ -146,6 +146,7 @@
             Some(Agent::Letta) => self.letta,
             Some(Agent::Maki) => self.maki,
             Some(Agent::Muse) => self.muse,
+            Some(Agent::Freebuff) => AgentSoundSetting::Default,
             None => AgentSoundSetting::Default,
         }
     }
--- a/src/detect/manifest.rs
+++ b/src/detect/manifest.rs
@@ -260,6 +260,7 @@
     ("qodercli", include_str!("manifests/qodercli.toml")),
     ("qwen", include_str!("manifests/qwen.toml")),
     ("copilot", include_str!("manifests/github-copilot.toml")),
+    ("freebuff", include_str!("manifests/freebuff.toml")),
 ];
 
 static MANIFEST_CACHE: OnceLock<RwLock<ManifestCache>> = OnceLock::new();
--- /dev/null
+++ b/src/detect/manifests/freebuff.toml
@@ -0,0 +1,71 @@
+id = "freebuff"
+version = "2026.08.27.1"
+min_engine_version = 2
+updated_at = "2026-08-27T00:00:00Z"
+aliases = ["codebuff"]
+
+# freebuff never emits a state-bearing OSC title: `setTerminalTitle` writes
+# `Freebuff: <first words of the prompt>` once per submit and leaves it there.
+# Everything below reads the status bar and the input box, which render in that
+# order directly above the bottom of the screen.
+
+[[rules]]
+id = "ask_user_form_blocked"
+state = "blocked"
+priority = 1000
+region = "bottom_non_empty_lines(16)"
+visible_blocker = true
+# The ask_user form replaces the whole input box, so its hints cannot coexist
+# with the composer placeholder below.
+all = [
+  { contains = ["navigate"] },
+  { contains = ["enter select"] },
+]
+
+[[rules]]
+id = "gate_banner_blocked"
+state = "blocked"
+priority = 950
+region = "bottom_non_empty_lines(16)"
+visible_blocker = true
+any = [
+  { contains = ["press enter to continue"] },
+  { contains = ["press enter to buy more credits"] },
+  { contains = ["credit spending is disabled"] },
+  { contains = ["press enter to login"] },
+  { contains = ["open this url", "to login:"] },
+]
+
+[[rules]]
+id = "status_bar_working"
+state = "working"
+priority = 900
+region = "bottom_non_empty_lines(12)"
+visible_working = true
+any = [
+  { line_regex = ['(?i)(?:^|\s)thinking\.\.\.\s*$'] },
+  { line_regex = ['(?i)(?:^|\s)working\.\.\.\s*$'] },
+  { line_regex = ['(?i)(?:^|\s)connecting\.\.\.\s*$'] },
+  { line_regex = ['(?i)(?:^|\s)retrying\.\.\.\s*$'] },
+  { contains = ["in line, starting soon"] },
+]
+
+[[rules]]
+id = "stop_button_working"
+state = "working"
+priority = 880
+region = "bottom_non_empty_lines(12)"
+visible_working = true
+# The status bar only mounts this button while waiting or streaming.
+contains = ["■ esc"]
+
+[[rules]]
+id = "composer_idle"
+state = "idle"
+priority = 100
+region = "bottom_non_empty_lines(8)"
+visible_idle = true
+any = [
+  { contains = ["enter a coding task"] },
+  { contains = ["ctrl-c to cancel queued messages"] },
+]
--- a/src/detect/mod.rs
+++ b/src/detect/mod.rs
@@ -65,10 +65,11 @@
     Letta,
     Maki,
     Muse,
+    Freebuff,
 }
 
 impl Agent {
-    pub const ALL: [Self; 24] = [
+    pub const ALL: [Self; 25] = [
         Self::Pi,
         Self::Claude,
         Self::Codex,
@@ -93,9 +94,10 @@
         Self::Letta,
         Self::Maki,
         Self::Muse,
+        Self::Freebuff,
     ];
 
-    pub const SCREEN_MANIFEST_AGENTS: [Self; 22] = [
+    pub const SCREEN_MANIFEST_AGENTS: [Self; 23] = [
         Self::Pi,
         Self::Claude,
         Self::Codex,
@@ -118,6 +120,7 @@
         Self::Letta,
         Self::Maki,
         Self::Muse,
+        Self::Freebuff,
     ];
 }
 
@@ -147,6 +150,7 @@
         Agent::Letta => "letta",
         Agent::Maki => "maki",
         Agent::Muse => "muse",
+        Agent::Freebuff => "freebuff",
     }
 }
 
@@ -182,6 +186,7 @@
         Agent::Letta => "letta",
         Agent::Maki => "maki",
         Agent::Muse => "muse",
+        Agent::Freebuff => "freebuff",
     }
 }
 
@@ -222,6 +227,7 @@
         "letta" | "letta-code" | "letta code" => Some(Agent::Letta),
         "maki" => Some(Agent::Maki),
         "muse" | "muse-code" | "muse-cli" => Some(Agent::Muse),
+        "freebuff" | "codebuff" => Some(Agent::Freebuff),
         _ if is_muse_versioned_binary(name) => Some(Agent::Muse),
         _ => None,
     }
@@ -343,6 +349,7 @@
             | ("herdr:qwen", "qwen")
             | ("herdr:letta", "letta")
             | ("herdr:antigravity_cli", "agy")
+            | ("herdr:freebuff", "freebuff")
     )
 }
 
--- a/src/terminal/state.rs
+++ b/src/terminal/state.rs
@@ -1334,6 +1334,7 @@
                 Some("startup" | "clear" | "resume" | "compact")
             ) | ("herdr:mastracode", "mastracode", Some("startup"))
                 | ("herdr:hermes", "hermes", Some("startup" | "new" | "resume"))
+                | ("herdr:freebuff", "freebuff", Some("startup" | "new" | "resume"))
                 | ("herdr:opencode", "opencode", Some("select"))
                 | ("herdr:pi", "pi", Some("new" | "resume" | "fork"))
                 | ("herdr:grok", "grok", Some("new"))
