diff --git a/src-tauri/src/overlay.rs b/src-tauri/src/overlay.rs
index 8fa038d..dbbc275 100644
--- a/src-tauri/src/overlay.rs
+++ b/src-tauri/src/overlay.rs
@@ -67,6 +67,9 @@ const OVERLAY_BOTTOM_OFFSET: f64 = 15.0;
 #[cfg(any(target_os = "windows", target_os = "linux"))]
 const OVERLAY_BOTTOM_OFFSET: f64 = 40.0;

+#[cfg(target_os = "linux")]
+const OVERLAY_EDGE_MARGIN: i32 = 16;
+
 #[cfg(target_os = "linux")]
 fn update_gtk_layer_shell_anchors(overlay_window: &tauri::webview::WebviewWindow) {
     let window_clone = overlay_window.clone();
@@ -83,10 +86,14 @@ fn update_gtk_layer_shell_anchors(overlay_window: &tauri::webview::WebviewWindow
                 OverlayPosition::Top => {
                     gtk_window.set_anchor(Edge::Top, true);
                     gtk_window.set_anchor(Edge::Bottom, false);
+                    gtk_window.set_layer_shell_margin(Edge::Top, OVERLAY_EDGE_MARGIN);
+                    gtk_window.set_layer_shell_margin(Edge::Bottom, 0);
                 }
                 OverlayPosition::Bottom => {
                     gtk_window.set_anchor(Edge::Bottom, true);
                     gtk_window.set_anchor(Edge::Top, false);
+                    gtk_window.set_layer_shell_margin(Edge::Bottom, OVERLAY_EDGE_MARGIN);
+                    gtk_window.set_layer_shell_margin(Edge::Top, 0);
                 }
             }
         }
diff --git a/src/overlay/RecordingOverlay.css b/src/overlay/RecordingOverlay.css
index 707b617..17296c3 100644
--- a/src/overlay/RecordingOverlay.css
+++ b/src/overlay/RecordingOverlay.css
@@ -199,8 +199,8 @@
   flex-direction: column;
   padding-top: 12px;
   padding-bottom: 0px;
-  padding-left: 14px;
-  padding-right: 14px;
+  padding-left: 18px;
+  padding-right: 18px;
   font-size: 15px;
   font-style: italic;
   line-height: 1.35;
@@ -243,7 +243,7 @@
    Same structure for pill & panel; the dot stays anchored left in both. */
 .sbase {
   height: var(--ov-base-h);
-  padding: 0 10px;
+  padding: 0 14px;
   display: grid;
   /* Equal side columns (min 22px to hold the cancel button) keep the center item
      — waveform or label — centered whether the pill is at min width or content-
